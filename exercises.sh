#!/usr/bin/env bash

# Write a script that creates a dir called bpl inside $HOME.
# Populate this directory with two subdirs, bin and scripts

cd ~

mkdir -p $HOME/bpl/bin/
mkdir -p $HOME/bpl/scripts
